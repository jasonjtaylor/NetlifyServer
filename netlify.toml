[build]
  publish = "."

# Redirect to your TAK server via Cloudflare Tunnel (Option A)
[[redirects]]
  from = "/*"
  to = "https://cited-yarn-scenic-medicines.trycloudflare.com/"
  status = 302
  force = true
