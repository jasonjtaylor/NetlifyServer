[build]
  publish = "."

# Redirect to your TAK server via Cloudflare Tunnel (Option A)
[[redirects]]
  from = "/*"
  to = "https://syntax-operating-resist-dates.trycloudflare.com/"
  status = 302
  force = true
